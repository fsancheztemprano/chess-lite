import { ResizableMatIconDirective } from './resizable-mat-icon.directive';

describe('ResizableMatIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableMatIconDirective();
    expect(directive).toBeTruthy();
  });
});
